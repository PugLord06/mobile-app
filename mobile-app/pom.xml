<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>za.ac.eduvos</groupId>
  <artifactId>eduv4895277-app</artifactId>
  <version>1.0.0</version>
  <packaging>apk</packaging>

  <name>Eduvos Android App (EDUV4895277)</name>
  <description>Android app for Deliverables 2-5. Author: Student number EDUV4895277</description>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.source>1.8</maven.compiler.source>
    <maven.compiler.target>1.8</maven.compiler.target>
    <android.plugin.version>4.6.0</android.plugin.version>
    <android.platform>33</android.platform>
    <android.buildTools.version>33.0.2</android.buildTools.version>
  </properties>

  <repositories>
    <repository>
      <id>central</id>
      <url>https://repo1.maven.org/maven2</url>
    </repository>
  </repositories>

  <dependencies>
    <!-- Android SDK -->
    <dependency>
      <groupId>com.google.android</groupId>
      <artifactId>android</artifactId>
      <version>4.1.1.4</version>
      <scope>provided</scope>
    </dependency>
  </dependencies>

  <build>
    <sourceDirectory>src/main/java</sourceDirectory>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.11.0</version>
        <configuration>
          <source>${maven.compiler.source}</source>
          <target>${maven.compiler.target}</target>
        </configuration>
      </plugin>
      <plugin>
        <groupId>com.simpligility.maven.plugins</groupId>
        <artifactId>android-maven-plugin</artifactId>
        <version>${android.plugin.version}</version>
        <configuration>
          <sdk>
            <platform>${android.platform}</platform>
            <buildTools>${android.buildTools.version}</buildTools>
          </sdk>
          <androidManifestFile>${project.basedir}/src/main/AndroidManifest.xml</androidManifestFile>
          <assetsDirectory>${project.basedir}/src/main/assets</assetsDirectory>
          <resourceDirectory>${project.basedir}/src/main/res</resourceDirectory>
          <nativeLibrariesDirectory>${project.basedir}/src/main/jniLibs</nativeLibrariesDirectory>
          <undeployBeforeDeploy>true</undeployBeforeDeploy>
          <proguard>
            <skip>true</skip>
          </proguard>
          <sign>
            <debug>true</debug>
          </sign>
          <zipalign>
            <skip>false</skip>
          </zipalign>
          <apkDebug>true</apkDebug>
        </configuration>
        <extensions>true</extensions>
      </plugin>
    </plugins>
  </build>
  
  <profiles>
    <!-- Profile for release builds -->
    <profile>
      <id>release</id>
      <build>
        <plugins>
          <plugin>
            <groupId>com.simpligility.maven.plugins</groupId>
            <artifactId>android-maven-plugin</artifactId>
            <configuration>
              <apkDebug>false</apkDebug>
              <sign>
                <debug>false</debug>
                <storeFile>${project.basedir}/keystore.jks</storeFile>
                <storePass>your-store-password</storePass>
                <keyAlias>your-key-alias</keyAlias>
                <keyPass>your-key-password</keyPass>
              </sign>
              <zipalign>
                <skip>false</skip>
              </zipalign>
              <proguard>
                <skip>false</skip>
                <config>proguard.cfg</config>
              </proguard>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project> 